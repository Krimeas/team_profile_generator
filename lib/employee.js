class Employee {
  constructor(name, id, email, ) {
    this.name = name;
    this.id = id;
    this.email = email;
  }

  getName() {
    console.log(`Name = ${this.name}`);
    return (this.name)
  }
  getId() {
    console.log(`Name = ${this.name}`);
    return (this.name)
  }
  getEmail() {
    console.log(`Name = ${this.name}`);
    return (this.name)
  }
  getRole() {
    console.log(`Name = ${this.name}`);
    return (this.name)
  }

}

module.exports = Employee;
